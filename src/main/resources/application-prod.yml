spring:
  datasource:
    url: jdbc:postgresql://db:5432/prod_db
    username: postgres
    password: postgres

  liquibase:
    contexts: prod

  data:
    redis:
      host: redis

comments-service:
  url:
    micro-comments:8082